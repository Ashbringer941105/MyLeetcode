package 设计模式.中介者模式;

public class Man extends Person {
    public Man(MarriageAgency agency, String name, int age, Sex sex, int requestAge) {
        super(agency, name, age, sex, requestAge);
    }
}
