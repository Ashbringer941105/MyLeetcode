package 设计模式.中介者模式;

public class Woman extends Person {
    public Woman(MarriageAgency agency, String name, int age, Sex sex, int requestAge) {
        super(agency, name, age, sex, requestAge);
    }
}
